version: '3'

services:

#######################################################
###                   HUGINN                        ###
#######################################################

  huginn:  
    image: huginn/huginn
    restart: always
    environment:
      - VIRTUAL_HOST=huginn2.mpdmail.de
      - LETSENCRYPT_HOST=huginn2.mpdmail.de
      - LETSENCRYPT_EMAIL=github@mpdmail.de
      - HUGINN_DATABASE_USERNAME=huginn
      - HUGINN_DATABASE_ADAPTER=postgresql
      - POSTGRES_PORT_5432_TCP_ADDR=huginn_postgres
      - POSTGRES_PORT_5432_TCP_PORT=5432
    ports:
      - "3000:3000"
    networks:
      - proxy-tier
      - huginn_net
    depends_on:
      - huginn_postgres
    env_file:
      - credentials.env

  huginn_postgres:
    image: postgres:10.2
    restart: always
    environment:
      - POSTGRES_USER=huginn
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    env_file:
      - credentials.env
    networks:
      - huginn_net

#######################################################

#######################################################
###                NEXTCLOUD                        ###
#######################################################


  nextcloud_db:
    image: mariadb
    restart: always
    volumes:
      - nextcloud_db:/var/lib/mysql
    env_file:
      - db.env
      - credentials.env

  nextcloud_redis:
    image: redis
    restart: always

  nextcloud_app:
    build: ./app
    restart: always
    volumes:
      - nextcloud:/var/www/html
    environment:
      - VIRTUAL_HOST=nextcloudtest.mpdmail.de
      - LETSENCRYPT_HOST=nextcloudtest.mpdmail.de
      - LETSENCRYPT_EMAIL=github@mpdmail.de
      - MYSQL_HOST=db
    env_file:
      - db.env
    depends_on:
      - nextcloud_db
      - nextcloud_redis
    networks:
      - proxy-tier
      - default

  nextcloud_cron:
    build: ./app
    restart: always
    volumes:
      - nextcloud:/var/www/html
    entrypoint: /cron.sh
    depends_on:
      - nextcloud_db
      - nextcloud_redis


#######################################################
###                NGINX-PROXY                      ###
#######################################################

  proxy:
    build: ./proxy
    restart: always
    ports:
      - "443:443"
      - "80:80"
    labels:
      com.github.jrcs.letsencrypt_nginx_proxy_companion.nginx_proxy: "true"
    volumes:
      - certs:/etc/nginx/certs:ro
      - conf.d:/etc/nginx/conf.d
      - vhost.d:/etc/nginx/vhost.d
      - html:/usr/share/nginx/html
      - /var/run/docker.sock:/tmp/docker.sock:ro
    networks:
      - proxy-tier

  letsencrypt-companion:
    image: jrcs/letsencrypt-nginx-proxy-companion
    restart: always
    volumes:
      - certs:/etc/nginx/certs
      - conf.d:/etc/nginx/conf.d
      - vhost.d:/etc/nginx/vhost.d
      - html:/usr/share/nginx/html
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - proxy-tier
    depends_on:
      - proxy

#######################################################


volumes:
  nextcloud_db:
  nextcloud:
  certs:
  conf.d:
  vhost.d:
  html:
  postgres-data:

networks:
  proxy-tier:
  huginn_net:
